/**
 * Stimulus generator template file.
 *
 * Purpose:
 * - Generate trial-specific stimulus data (arrays, positions, colors, etc.).
 * - Keep this file focused on data generation only.
 *
 * Suggested usage:
 * 1. Read options/config for current trial type.
 * 2. Generate stimulus arrays with your own randomization logic.
 * 3. Return plain data objects that can be consumed by layout/trial builders.
 *
 * Example output shape (commented):
 * [
 *   { id: "stim-1", value: "A", condition: 1, position: [0, 0] },
 *   { id: "stim-2", value: "K", condition: 1, position: [100, 0] },
 * ]
 */

/**
 * Trial class/factory template file.
 *
 * Purpose:
 * - Build a complete trial from:
 *   1) generated stimuli data (`stimuli.ts`)
 *   2) psychophysics layout objects (`layout.ts`)
 * - Optionally return a jsPsych timeline snippet for one trial.
 *
 * Suggested usage:
 * 1. Create a class (or function) that accepts trial options.
 * 2. Call your stimulus generator.
 * 3. Call your layout builder to produce psychophysics objects.
 * 4. Assemble and return a single-trial bundle/timeline.
 *
 * This template intentionally leaves implementation to project-specific logic.
 */

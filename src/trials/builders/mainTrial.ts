/**
 * Main/normal trial builder template file.
 *
 * Purpose:
 * - Define how to construct one "real" (non-practice) trial.
 * - Select conditions and pass options into your trial class/factory.
 *
 * Suggested usage:
 * 1. Compute/select condition for the current index.
 * 2. Set `isPractice = false`.
 * 3. Call your trial builder from `trial.ts`.
 */
